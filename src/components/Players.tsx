import React, { FC } from "react";

const Players: FC = () => {

    //const classes = useStyles();

    return (
        <>
            <span>Players!</span>
        </>
    );
};
export default Players;