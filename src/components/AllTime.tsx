import React, { FC } from "react";

const AllTime: FC = () => {

    //const classes = useStyles();

    return (
        <>
            <span>Alltime!</span>
        </>
    );
};
export default AllTime;